namespace oo_cookies
{
    public class cookie
    {
        public int weight;
        public string shape;
        public colour colour;
        public cookie(int weight, string shape, colour colour)
        {
            this.weight = weight;
            this.shape = shape;
            this.colour = colour;
        }
    }
}